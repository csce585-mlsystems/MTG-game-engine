services:
  - type: web
    name: mtg-land-simulator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && uvicorn app.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
